{
  "schemaVersion": 1,
  "app": {
    "key": "customer-portal",
    "displayName": "Customer Portal",
    "description": "Self-service account, orders and support for customers."
  },
  "version": "1.4.0",
  "source": { "branch": "main", "commit": "c2d3e4f" },
  "exportedAt": "2026-08-12T10:00:00Z",
  "exportedBy": "studio@apptor",
  "auth": {
    "required": true,
    "authProvider": "Apptor-Id",
    "expectedClaims": ["email"],
    "tokenStrategy": "apptor-minted",
    "configKeys": []
  },
  "integrations": [
    {
      "collection": "billing-api",
      "authType": "oauth2-client-credentials",
      "configKeys": [
        { "key": "base-url", "label": "Base URL", "type": "url", "required": true },
        { "key": "client-id", "label": "Client ID", "type": "text", "required": true },
        { "key": "client-secret", "label": "Client secret", "type": "secret", "required": true }
      ]
    },
    {
      "collection": "crm-api",
      "authType": "caller-bearer",
      "configKeys": [{ "key": "base-url", "label": "Base URL", "type": "url", "required": true }]
    },
    {
      "collection": "notification-api",
      "authType": "api-key",
      "configKeys": [
        { "key": "base-url", "label": "Base URL", "type": "url", "required": true },
        { "key": "api-key", "label": "API Key", "type": "secret", "required": true },
        { "key": "client-id", "label": "Client ID", "type": "text", "required": true }
      ]
    }
  ]
}
