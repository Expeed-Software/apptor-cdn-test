{
  "schemaVersion": 1,
  "app": {
    "key": "customer-portal",
    "displayName": "Customer Portal",
    "description": "Self-service account, orders and support for customers."
  },
  "version": "1.0.0",
  "source": { "branch": "main", "commit": "a1b2c3d" },
  "exportedAt": "2026-07-01T10:00:00Z",
  "exportedBy": "studio@apptor",
  "auth": {
    "required": true,
    "authProvider": "auth0",
    "expectedClaims": ["email"],
    "tokenStrategy": "apptor-minted",
    "configKeys": [
      {
        "key": "issuer-url",
        "label": "Issuer URL",
        "type": "url",
        "required": true,
        "description": "The web address people are sent to when they sign in."
      },
      { "key": "client-id", "label": "Client ID", "type": "text", "required": true },
      {
        "key": "client-secret",
        "label": "Client Secret",
        "type": "secret",
        "required": true,
        "description": "Stored encrypted. We never show it again — paste a new one to replace it."
      },
      {
        "key": "scopes",
        "label": "Scopes",
        "type": "multiselect",
        "required": true,
        "options": ["openid", "profile", "email", "address", "phone", "offline_access"],
        "description": "The pieces of a profile the app needs, such as name and email address."
      },
      { "key": "authorize-url", "label": "Authorize URL", "type": "url", "required": true },
      { "key": "token-url", "label": "Token URL", "type": "url", "required": false }
    ]
  },
  "integrations": [
    {
      "collection": "billing-api",
      "authType": "oauth2-client-credentials",
      "configKeys": [
        {
          "key": "base-url",
          "label": "Base URL",
          "type": "url",
          "required": true,
          "description": "Where this system lives. Ask whoever runs it if you are not sure."
        },
        {
          "key": "token-url",
          "label": "Token URL",
          "type": "url",
          "required": true,
          "description": "Where this system issues access tokens."
        },
        { "key": "client-id", "label": "Client ID", "type": "text", "required": true },
        { "key": "client-secret", "label": "Client Secret", "type": "secret", "required": false }
      ]
    },
    {
      "collection": "notifications-api",
      "authType": "static-bearer",
      "configKeys": [
        {
          "key": "base-url",
          "label": "Base URL",
          "type": "url",
          "required": true,
          "description": "Where this system lives. Ask whoever runs it if you are not sure."
        },
        {
          "key": "token",
          "label": "Access Token",
          "type": "secret",
          "required": true,
          "description": "Sent as an Authorization: Bearer header on every request."
        }
      ]
    }
  ]
}
